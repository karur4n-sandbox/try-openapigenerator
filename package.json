{
  "name": "try-openapigenerator",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate:php": "openapi-generator-cli generate -i ./schema.openapi.json -g php -o ./php",
    "generate:laravel": "openapi-generator-cli generate -i ./schema.openapi.json -g php-laravel -o ./php-laravel"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.13.7"
  }
}
